# Plan: Completion Pill + ModeToggle Cleanup

## Summary

Two small UI changes to reduce confusion in the header:

1. Rename the "partial" badge text to "Partial Completion" in `DayCompletionBadge`
2. Remove the `ModeToggle` button from the header (mode is already settable in Settings)

---

## Changes

### 1. `components/counter/DayCompletionBadge.tsx` (line 24)
- Change the label text from `partial` → `Partial Completion`

### 2. `app/page.tsx`
- Remove the `import { ModeToggle }` import (line 5)
- Remove the `<ModeToggle />` JSX element (line 70)

### 3. `components/counter/ModeToggle.tsx`
- Delete the file (no longer referenced anywhere)

---

## Notes

- The "complete" label in `DayCompletionBadge` (line 17) is left as-is — "complete" is unambiguous on its own.
- The `ModeToggle` only toggles `sessionStore.mode`; the Settings sheet already controls `settingsStore.defaultMode` which seeds `sessionStore.mode` on each new session. Removing the in-header toggle means users set mode once via Settings rather than per-session via the button.
- No logic changes; purely UI cleanup.
