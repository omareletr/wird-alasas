---
phase: 00-foundation-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01
  - 00-02
files_modified:
  - lib/storage/schema.ts
  - lib/storage/idb.ts
  - lib/storage/localStorage.ts
  - lib/store/sessionStore.ts
  - lib/store/settingsStore.ts
  - components/StoreHydration.tsx
  - app/layout.tsx
  - package.json
autonomous: false
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Active session (counts, mode, activeIndex, sessionStartedAt) persists to localStorage and rehydrates on page reload"
    - "User settings (defaultMode, location) persist to localStorage and rehydrate on page reload"
    - "Daily history records can be written to IndexedDB and read back"
    - "Schema migration runs without error when schemaVersion key is absent from localStorage"
    - "No React hydration mismatch errors appear in the browser console on first load"
    - "The app builds and runs with Zustand, idb, and the storage layer wired in"
  artifacts:
    - path: "lib/storage/schema.ts"
      provides: "TypeScript interfaces: ActiveSession, UserSettings, DailyRecord"
      exports:
        - "ActiveSession"
        - "UserSettings"
        - "DailyRecord"
        - "DHIKR_COUNT"
    - path: "lib/storage/idb.ts"
      provides: "Typed IndexedDB helper — addDailyRecord, getAllDailyRecords, getDailyRecord"
      exports:
        - "addDailyRecord"
        - "getAllDailyRecords"
        - "getDailyRecord"
    - path: "lib/storage/localStorage.ts"
      provides: "Schema version check and migration runner"
      exports:
        - "runMigrationIfNeeded"
    - path: "lib/store/sessionStore.ts"
      provides: "Zustand sessionStore with persist middleware (skipHydration: true)"
      exports:
        - "useSessionStore"
    - path: "lib/store/settingsStore.ts"
      provides: "Zustand settingsStore with persist middleware (skipHydration: true)"
      exports:
        - "useSettingsStore"
    - path: "components/StoreHydration.tsx"
      provides: "Client component that rehydrates both stores inside useEffect"
  key_links:
    - from: "app/layout.tsx"
      to: "components/StoreHydration.tsx"
      via: "mounted as child of <body>"
      pattern: "StoreHydration"
    - from: "components/StoreHydration.tsx"
      to: "lib/store/sessionStore.ts"
      via: "useSessionStore.persist.rehydrate() in useEffect"
      pattern: "persist\\.rehydrate"
    - from: "lib/store/sessionStore.ts"
      to: "lib/storage/schema.ts"
      via: "imports ActiveSession interface"
      pattern: "ActiveSession"
    - from: "lib/store/settingsStore.ts"
      to: "lib/storage/schema.ts"
      via: "imports UserSettings interface"
      pattern: "UserSettings"
---

<objective>
Establish the complete local-first storage layer: typed schema, IndexedDB helper (idb), localStorage migration, and two Zustand stores wired into layout with SSR-safe hydration.

Purpose: This is FOUND-01 in full — all app data stored locally with no auth required. Phase 1 will read and write the session store on every tap. If the store hydration is wrong, counts will reset on every page load.
Output: Two persisted Zustand stores (session + settings), an IndexedDB helper for daily history, a schema migration runner, and a `StoreHydration` client component mounted in layout.
</objective>

<execution_context>
@/Users/omareletr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omareletr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-foundation-cleanup/00-CONTEXT.md
@.planning/phases/00-foundation-cleanup/00-RESEARCH.md
@.planning/phases/00-foundation-cleanup/00-01-SUMMARY.md
@.planning/phases/00-foundation-cleanup/00-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts this plan must implement. Derive from decisions in CONTEXT.md and RESEARCH.md. -->

From CONTEXT.md (locked decisions):
```typescript
// Active session shape (canonical — do not alter field names)
interface ActiveSession {
  counts: Record<0 | 1 | 2 | 3, number>;  // one count per dhikr index
  mode: "full" | "shortened";
  sessionStartedAt: number | null;          // Unix ms timestamp, set when session starts
  activeIndex: 0 | 1 | 2 | 3;             // dhikr the user was last viewing
}

// User settings shape
interface UserSettings {
  defaultMode: "full" | "shortened";
  location: { latitude: number; longitude: number } | null;
}
```

From RESEARCH.md (Pattern 2 — idb typed schema):
```typescript
// DailyRecord shape for IndexedDB
interface DailyRecord {
  dayKey: string;                            // "2026-05-30" YYYY-MM-DD
  counts: Record<0 | 1 | 2 | 3, number>;
  mode: "full" | "shortened";
  completedAt: number;                       // Unix ms timestamp of archival
}
```

From RESEARCH.md (Pattern 1 — Zustand persist key):
- localStorage persist key for session: `"wird-session"`
- localStorage persist key for settings: `"wird-settings"`
- Zustand persist version: `1` for both stores
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Zustand + idb, define schema types, build storage helpers</name>
  <files>
    lib/storage/schema.ts
    lib/storage/idb.ts
    lib/storage/localStorage.ts
    package.json
  </files>
  <action>
    **Install dependencies:**
    ```
    npm install zustand idb
    ```

    **lib/storage/schema.ts** — Canonical TypeScript interfaces. Export:
    - `DHIKR_COUNT = 4` (constant, avoids magic numbers downstream)
    - `type DhikrIndex = 0 | 1 | 2 | 3`
    - `interface ActiveSession` — exactly as in the interfaces block above
    - `interface UserSettings` — exactly as in the interfaces block above
    - `interface DailyRecord` — exactly as in the interfaces block above

    **lib/storage/idb.ts** — Typed IndexedDB helpers using `idb` v8:
    - Define `WirdDB extends DBSchema` with a `"daily-records"` object store: key is `dayKey: string`, value is `DailyRecord` from schema.ts, index `"by-dayKey"` on `dayKey`
    - DB name: `"wird-alasas"`, DB version: `1`
    - Cache the `openDB()` promise at module scope (singleton) to avoid re-opening on every read/write
    - Export:
      - `addDailyRecord(record: DailyRecord): Promise<string>` — upserts using `db.put`
      - `getAllDailyRecords(): Promise<DailyRecord[]>`
      - `getDailyRecord(dayKey: string): Promise<DailyRecord | undefined>`

    **lib/storage/localStorage.ts** — Schema migration runner (runs on app boot):
    - `CURRENT_SCHEMA_VERSION = 1`, key `"wird-schema-version"`
    - Export `runMigrationIfNeeded(): void`
    - Migration v0→v1: remove any legacy Supabase auth key `"supabase.auth.token"` from localStorage
    - Pattern from RESEARCH.md Pattern 4 (sequential migrations loop)

    All files should be server-side-safe: `idb.ts` and `localStorage.ts` must guard against running in SSR (check `typeof window !== "undefined"` before accessing `localStorage` or opening IndexedDB).
  </action>
  <verify>
    <automated>cd "/Users/omareletr/Documents/Claude Code Projects/wird_alasas" && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `lib/storage/schema.ts` exports `ActiveSession`, `UserSettings`, `DailyRecord`, `DhikrIndex`, `DHIKR_COUNT`
    - `lib/storage/idb.ts` exports `addDailyRecord`, `getAllDailyRecords`, `getDailyRecord`
    - `lib/storage/localStorage.ts` exports `runMigrationIfNeeded`
    - `zustand` and `idb` appear in `package.json` dependencies
    - `npm run build` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Zustand stores + StoreHydration component wired into layout</name>
  <files>
    lib/store/sessionStore.ts
    lib/store/settingsStore.ts
    components/StoreHydration.tsx
    app/layout.tsx
  </files>
  <action>
    **lib/store/sessionStore.ts** — Active session Zustand store:
    - `"use client"` directive at top (Zustand stores with persist must be Client Module compatible)
    - Import `ActiveSession` from `@/lib/storage/schema`
    - Use `create<SessionStore>()(persist(..., { name: "wird-session", storage: createJSONStorage(() => localStorage), skipHydration: true, version: 1 }))`
    - Store shape `SessionStore` extends `ActiveSession` with actions:
      - `incrementCount(index: DhikrIndex): void` — increments `counts[index]` by 1
      - `setCount(index: DhikrIndex, value: number): void` — direct set
      - `setMode(mode: "full" | "shortened"): void`
      - `setActiveIndex(index: DhikrIndex): void`
      - `setSessionStartedAt(ts: number): void`
      - `reset(): void` — resets counts to `{ 0: 0, 1: 0, 2: 0, 3: 0 }` and `sessionStartedAt` to null
    - Default values: `counts: { 0: 0, 1: 0, 2: 0, 3: 0 }`, `mode: "full"`, `sessionStartedAt: null`, `activeIndex: 0`
    - Export `useSessionStore`

    **lib/store/settingsStore.ts** — User settings Zustand store:
    - `"use client"` directive
    - Import `UserSettings` from `@/lib/storage/schema`
    - Use `create<SettingsStore>()(persist(..., { name: "wird-settings", storage: createJSONStorage(() => localStorage), skipHydration: true, version: 1 }))`
    - Store shape `SettingsStore` extends `UserSettings` with actions:
      - `setDefaultMode(mode: "full" | "shortened"): void`
      - `setLocation(loc: { latitude: number; longitude: number } | null): void`
    - Default values: `defaultMode: "full"`, `location: null`
    - Export `useSettingsStore`

    **components/StoreHydration.tsx** — SSR-safe rehydration:
    ```typescript
    "use client";
    import { useEffect } from "react";
    import { useSessionStore } from "@/lib/store/sessionStore";
    import { useSettingsStore } from "@/lib/store/settingsStore";
    import { runMigrationIfNeeded } from "@/lib/storage/localStorage";

    export function StoreHydration() {
      useEffect(() => {
        runMigrationIfNeeded();
        useSessionStore.persist.rehydrate();
        useSettingsStore.persist.rehydrate();
      }, []);
      return null;
    }
    ```
    Call `runMigrationIfNeeded()` before rehydration so any schema changes happen before stores read stale data.

    **app/layout.tsx** — Mount `StoreHydration` inside `<body>`:
    - Import `StoreHydration` from `@/components/StoreHydration`
    - Place `<StoreHydration />` as the first child inside `<body>`, before `{children}`
    - Keep all existing layout content (fonts, metadata, dark class)

    Run `npm run build` and `npm run test:run` to confirm everything is wired correctly.
  </action>
  <verify>
    <automated>cd "/Users/omareletr/Documents/Claude Code Projects/wird_alasas" && npm run build 2>&1 | tail -15 && npm run test:run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `lib/store/sessionStore.ts` exports `useSessionStore` with all five actions
    - `lib/store/settingsStore.ts` exports `useSettingsStore` with two actions
    - Both stores use `skipHydration: true` in persist options
    - `components/StoreHydration.tsx` calls both `persist.rehydrate()` and `runMigrationIfNeeded()` inside `useEffect`
    - `app/layout.tsx` renders `<StoreHydration />` inside `<body>`
    - `npm run build` exits 0
    - `npm run test:run` exits 0 (all existing tests still pass)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — dark app loads cleanly with no hydration errors</name>
  <what-built>
    Complete local-first storage layer:
    - Typed schema (schema.ts)
    - IndexedDB helper (idb.ts) for daily history
    - localStorage migration runner
    - Session store (persists counts, mode, activeIndex, sessionStartedAt)
    - Settings store (persists defaultMode, location)
    - StoreHydration component mounted in layout
    - Dark branded placeholder page
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Confirm: black background, Arabic "وِرد الأساس", "coming soon" text
    3. Open browser DevTools → Console: confirm no React hydration errors
    4. Open DevTools → Application → Local Storage: confirm `wird-session` and `wird-settings` keys are NOT there yet (they appear after first interaction, not immediately)
    5. Confirm no errors appear in the console on page load
    6. Run `npm run test:run` in the terminal — confirm all tests pass
  </how-to-verify>
  <action>Human verification step. Run the dev server and visually confirm the app renders correctly with no console errors.</action>
  <verify>
    <automated>MISSING — visual check required</automated>
  </verify>
  <done>App loads at http://localhost:3000 with dark background and Arabic name, no hydration errors in console, all tests pass.</done>
  <resume-signal>Type "approved" if the app loads correctly with no errors, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` passes (exit 0)
2. `npm run lint` passes (exit 0)
3. `npm run test:run` passes (all tests green)
4. `grep -r "skipHydration" lib/store/` confirms both stores have SSR-safe hydration
5. `grep "StoreHydration" app/layout.tsx` confirms the component is mounted
</verification>

<success_criteria>
- Active session persists to localStorage key `"wird-session"` and rehydrates after page reload
- Settings persist to localStorage key `"wird-settings"` and rehydrate after page reload
- IndexedDB `addDailyRecord` + `getAllDailyRecords` API exists and is typed
- Schema migration runs on boot without errors
- No React hydration mismatch (verified manually by human)
- All tests pass, build passes, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-cleanup/00-03-SUMMARY.md`
</output>
