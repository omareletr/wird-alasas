---
phase: 00-foundation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - app/page.tsx
  - app/layout.tsx
  - app/globals.css
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "The app builds with zero Supabase-related import errors"
    - "No Supabase or auth-related files or directories remain in the repo"
    - "Root middleware.ts no longer imports from lib/supabase/"
    - "app/page.tsx renders a minimal branded placeholder on a pure black background"
    - "layout.tsx shows title 'wird al-asas' and the correct description"
    - "globals.css sets --background to oklch(0 0 0) and --foreground to oklch(1 0 0)"
  artifacts:
    - path: "middleware.ts"
      provides: "No-op Next.js middleware (no Supabase dependency)"
    - path: "app/page.tsx"
      provides: "Branded placeholder page — dark background, white text"
    - path: "app/layout.tsx"
      provides: "Updated metadata and dark theme class on <html>"
    - path: "app/globals.css"
      provides: "Always-dark CSS variable overrides"
  key_links:
    - from: "middleware.ts"
      to: "(none)"
      via: "deleted import of @/lib/supabase/middleware"
      pattern: "no import.*supabase"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "dark CSS token override"
      pattern: "--background.*oklch\\(0"
---

<objective>
Strip all Supabase and auth scaffolding from the Next.js starter template, and apply the dark base theme and branded placeholder that Phase 1 will build on.

Purpose: The starter template has Supabase auth baked in (middleware, lib/supabase/, login, protected, auth routes). Every file in Phase 1 would hit broken imports if this is not removed first. This plan makes the codebase valid and clean before any feature work begins.
Output: A working Next.js 15 app with no Supabase dependencies, a dark branded placeholder page, correct layout metadata, and dark CSS variables in globals.css.
</objective>

<execution_context>
@/Users/omareletr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omareletr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation-cleanup/00-CONTEXT.md
@.planning/phases/00-foundation-cleanup/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Supabase files, uninstall packages, fix middleware</name>
  <files>
    middleware.ts
    lib/supabase/client.ts
    lib/supabase/server.ts
    lib/supabase/middleware.ts
    app/login/page.tsx (and directory)
    app/auth/confirm/route.ts (and directory)
    app/auth/auth-code-error/ (and directory)
    app/protected/page.tsx (and directory)
    package.json
  </files>
  <action>
    1. Delete the following directories entirely (use `rm -rf`):
       - `lib/supabase/`
       - `app/login/`
       - `app/auth/`
       - `app/protected/`

    2. Uninstall Supabase packages:
       ```
       npm uninstall @supabase/ssr @supabase/supabase-js
       ```

    3. Replace `middleware.ts` root file entirely with a minimal pass-through that has no imports and an empty matcher so it never runs:
       ```typescript
       import { type NextRequest, NextResponse } from "next/server";

       export function middleware(_request: NextRequest) {
         return NextResponse.next();
       }

       export const config = {
         matcher: [],
       };
       ```
       Rationale: An empty matcher `[]` would crash the dev server on some Next.js versions — use a real function body that returns `NextResponse.next()` with an empty matcher array. This is safe; the middleware runs on no paths.

    4. Run `npm run build` to confirm no broken imports remain. Fix any remaining import errors if found (search for any other file importing from `@/lib/supabase/`).
  </action>
  <verify>
    <automated>cd "/Users/omareletr/Documents/Claude Code Projects/wird_alasas" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `lib/supabase/` directory does not exist
    - `app/login/`, `app/auth/`, `app/protected/` directories do not exist
    - `package.json` contains neither `@supabase/ssr` nor `@supabase/supabase-js`
    - `middleware.ts` has no Supabase import
    - `npm run build` exits 0 with no module-not-found errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Dark theme, branded placeholder page, layout metadata</name>
  <files>
    app/globals.css
    app/page.tsx
    app/layout.tsx
  </files>
  <action>
    **app/globals.css** — Apply the always-dark base theme.
    The file uses Tailwind v4 CSS variables. Find the `:root` block (or the block that sets `--background` and `--foreground`) and change:
    - `--background` → `oklch(0 0 0)` (pure black, maps to `#000000`)
    - `--foreground` → `oklch(1 0 0)` (pure white)
    Remove or leave empty the `.dark` block — the app is always dark, no toggle needed.
    Do NOT add raw `background: #000000` to `:root`; update the CSS variable so Tailwind utility classes (`bg-background`, `text-foreground`) continue to work.

    **app/layout.tsx** — Update metadata and add `dark` class to `<html>`:
    ```typescript
    export const metadata: Metadata = {
      title: "wird al-asas",
      description: "Complete your daily wird al-asas — four adhkar, every day.",
    };
    ```
    Add `className="dark"` to the `<html>` element so any shadcn components that use the `.dark` selector pick up dark tokens.
    Keep the existing Geist font setup.

    **app/page.tsx** — Replace the Next.js starter content with a minimal branded placeholder.
    Use only existing shadcn/ui + Tailwind; no new components needed. The placeholder should show:
    - The app name "wird al-asas" in Arabic: "وِرد الأساس" — rendered with `dir="rtl"` and `lang="ar"` on its container
    - A subtitle in English: "coming soon"
    - Background must be black (`bg-background`), text white (`text-foreground`)
    - Center everything vertically and horizontally on the full viewport

    Example structure (adjust styling as needed):
    ```tsx
    export default function Home() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center bg-background text-foreground">
          <div className="text-center space-y-4">
            <h1 dir="rtl" lang="ar" className="text-4xl font-light tracking-wide">
              وِرد الأساس
            </h1>
            <p className="text-sm text-muted-foreground uppercase tracking-widest">
              coming soon
            </p>
          </div>
        </main>
      );
    }
    ```
    Remove all existing Next.js starter imports (Image, Link, etc.) that are no longer needed.

    Run `npm run build` and `npm run lint` after to confirm clean.
  </action>
  <verify>
    <automated>cd "/Users/omareletr/Documents/Claude Code Projects/wird_alasas" && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `globals.css` has `--background: oklch(0 0 0)` and `--foreground: oklch(1 0 0)` (or equivalent)
    - `layout.tsx` metadata shows title "wird al-asas"
    - `layout.tsx` `<html>` element has `className="dark"`
    - `app/page.tsx` renders Arabic name and "coming soon" with no Next.js starter boilerplate
    - `npm run build` and `npm run lint` both exit 0
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` passes with exit 0
2. `npm run lint` passes with no errors
3. `grep -r "supabase" /Users/omareletr/Documents/Claude\ Code\ Projects/wird_alasas/app /Users/omareletr/Documents/Claude\ Code\ Projects/wird_alasas/lib /Users/omareletr/Documents/Claude\ Code\ Projects/wird_alasas/middleware.ts 2>/dev/null` returns nothing
4. `lib/supabase/`, `app/login/`, `app/auth/`, `app/protected/` directories do not exist
</verification>

<success_criteria>
- Zero Supabase files, imports, or packages remain
- Middleware is a no-op pass-through (no auth logic)
- App page renders branded dark placeholder with Arabic name
- Layout metadata correctly identifies the app
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-cleanup/00-01-SUMMARY.md`
</output>
