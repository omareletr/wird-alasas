import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.wirdalasas.app",
  appName: "Wird Al-Asas",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
